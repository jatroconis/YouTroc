package com.youtroc.core.ui.theme

import androidx.compose.ui.graphics.Color

/** Brand red, sampled from the logo badge. */
val YouTrocRed = Color(0xFFF80202)

/** Near-black base surface for the 10-foot dark UI. */
val AlmostBlack = Color(0xFF0F0F0F)

/** Slightly lifted surface for elevated content. */
val ElevatedSurface = Color(0xFF1E1E1E)

val OnDark = Color(0xFFF1F1F1)
val OnDarkMuted = Color(0xFFAFAFAF)
